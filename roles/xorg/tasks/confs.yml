---

- name: Xdefaults
  copy:
    src: Xdefaults
    dest: "{{ user.home }}/.Xdefaults"
    owner: "{{ user.name }}"

- name: xinitrc
  copy:
    src: xinitrc
    dest: "{{ user.home }}/.xinitrc"
    owner: "{{ user.name }}"
