[user]
	email = "{{ git.email }}"
	name = "{{ git.name }}"
[pull]
	rebase = true
[alias]
        lg = "!git log --graph --all --oneline"
        sttaus = "!git status"
        wdiff = diff --color-words
