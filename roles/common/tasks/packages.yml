---

- name: Install common tools
  pacman:
    update_cache: yes
    state: present
    name:
      - htop
      - tree
      - zsh
      - vim
      - tcpdump
      - curl
      - screen
      - git
      - ansible
      - pkgfile
      - mlocate
      - bind-tools

- aur:
    use: trizen
    name:
      - oh-my-zsh-git
      - google-chrome
  become: yes
  become_user: aurbuilder
