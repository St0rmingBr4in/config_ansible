---

- name: vimrc
  copy:
    src: vimrc
    dest: "{{ user.home }}/.vimrc"
    owner: "{{ user.name }}"

- name: zshrc
  copy:
    src: zshrc
    dest: "{{ user.home }}/.zshrc"
    owner: "{{ user.name }}"

