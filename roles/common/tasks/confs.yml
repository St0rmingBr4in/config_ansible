---

- name: zshrc
  copy:
    src: zshrc
    dest: "{{ user.home }}/.zshrc"
    owner: "{{ user.name }}"

- name: gitconfig
  copy:
    src: gitconfig
    dest: "{{ user.home }}/.gitconfig"
    owner: "{{ user.name }}"

- name: pacman.conf
  copy:
    src: pacman.conf
    dest: /etc/pacman.conf
