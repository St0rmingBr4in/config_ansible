---

- name: Create user
  user:
    name={{ user.name }}
    groups={{ user.groups }}
    append={{ user.append }}
    home={{ user.home }}
    password={{ user.password|password_hash('sha512') }}

#- name: Add ssh keys
